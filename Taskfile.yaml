version: '3'

tasks:
  users:
    cmds:
      - protoc -I proto proto/users.proto --go_out=./gen/users --go_opt=paths=source_relative --go-grpc_out=./gen/users --go-grpc_opt=paths=source_relative
  drivers:
    cmds:
      - protoc -I proto proto/drivers.proto --go_out=./gen/drivers --go_opt=paths=source_relative --go-grpc_out=./gen/drivers --go-grpc_opt=paths=source_relative

